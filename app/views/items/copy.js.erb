var item = $("<%= @item.as_js_item(self) %>");

if(item.data("parentId") == MyFile.current_item_id) {
  MyFile.append_item(item)
} else {
  var parent = $("<%= @parent.as_js_item(self) %>");
  MyFile.refresh_item(parent)
}
